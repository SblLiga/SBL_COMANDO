# Legacy frontend stub

This folder is a **minimal React scaffold** left from early scaffolding.

**Do not add product features here.**

The live application is:

```text
../base44-app/
```

CodeBuild (`backend/buildspec.yml`) builds **`base44-app/`**, not this directory.
