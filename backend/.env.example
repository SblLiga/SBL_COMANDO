# Application
APP_ENV=dev
LOG_LEVEL=DEBUG
PORT=8000

# PostgreSQL (RDS)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sbl_dev
DB_USER=sbl_admin
DB_PASSWORD=change_me
DB_SSLMODE=prefer

# בענן — מוזרק ע"י Terraform ל-Elastic Beanstalk (אין צורך להגדיר ידנית)
# DB_SECRET_ARN=arn:aws:secretsmanager:eu-north-1:...
# DB_SSLMODE=require
# RUN_DB_MIGRATIONS=true

# Frontend (Vite) — ראה frontend/.env.example
# VITE_API_URL=http://localhost:8000
